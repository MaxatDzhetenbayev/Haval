import { ModelsList, HeroSwiper } from "@/widgets";
import { Box } from "@mui/material";
import React from "react";

export const Home = () => {
  return (
    <Box>
      <HeroSwiper />
      <ModelsList />
    </Box>
  );
};
